{
  "name": "jtcob",
  "version": "0.1.0",
  "description": "JSON to CSV on Browser",
  "main": "index.js",
  "scripts": {
    "minify": "uglifyjs --compress --mangle -- index.js > index.min.js",
    "fmt": "prettier --write '**/*.{js,json,md,html}'",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "json",
    "to",
    "csv",
    "convert",
    "browser"
  ],
  "author": "shinshin86 <shinshin86npm@gmail.com> (https://github.com/shinshin86)",
  "license": "MIT",
  "devDependencies": {
    "prettier": "^2.0.5",
    "uglify-js": "^3.10.0"
  },
  "prettier": {
    "singleQuote": true
  }
}
